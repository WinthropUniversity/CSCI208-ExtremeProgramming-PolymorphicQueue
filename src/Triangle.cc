#include"Triangle.h"
#include<iostream>

double Triangle::GetBase(double inBase)
{
    base = inBase;
    return base;
}

double Triangle::GetHeight(double inHeight)
{
    height = inHeight;
    return height;
}
